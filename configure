#!/usr/bin/env ruby

$current_dir = File.dirname(File.expand_path(__FILE__))
Dir[File.join($current_dir, "setup/*.rb")].each do |file|
  require_relative(file)
end

module_type = ARGV.shift
pod_name = ARGV.shift
Pod::TemplateConfigurator.new(pod_name, module_type).run
